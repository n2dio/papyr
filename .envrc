# Auto-load the nix-shell (shell.nix) when you cd into this directory.
# Run `direnv allow` once to enable it.
#
# `use nix` works with stock direnv; if you have nix-direnv configured in your
# ~/.config/direnv/direnvrc it transparently caches the env for fast re-entry.
use nix
watch_file shell.nix
